import { RestrictedDirective } from './restricted.directive';

describe('RestrictedDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedDirective();
    expect(directive).toBeTruthy();
  });
});
